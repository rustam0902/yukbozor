node_modules
dist
!dist/index.cjs
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
!yukbozor-deploy.tar.gz
# User uploaded files
/uploads/
# Environment variables and secrets — never commit
.env
.env.*
!.env.example
# Replit agent assets — large binary files, not source code
attached_assets/
ziHUl9wQ
zijtzO8Z
uz-
# Binary archives (not source code)
*.zip
*.rar
*.tar
*.gz

# Private keys and secrets — never commit
*.key
*.jks
*.pem
*.p12
e-imzo-upload/keys/
e-imzo-upload/config.properties
